re模块高级用法
“函数和功能介绍”：
1. search()： 会扫描整个字符串并返回第一个成功的匹配。
2. findall()以列表形式返回匹配到的字符串
3.sub()将匹配到的数据进行替换
4.split()根据匹配进行切割字符串，并返回一个列表。

         匹配范围                返回结果      匹配次数

match    从头开始                object  一次  最不常用

search   返回第一个匹配成功的匹配项  object  一次  较常用

findall   匹配所有，所以匹配成功项     列表    所有  最常用

