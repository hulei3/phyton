了解协程
“概念”：协程是python个中另外一种实现多任务的方式，比线程占用的执行单元更小，协程是单线程下的并发，又称微线程。
“好处”：1.在进程和线程中，调度是由cpu来决定的，但是在协程可以实现，你想让它执行到哪里它就执行到哪里。
 2.在一个协程中的某个函数，可以在任何地方保存当前函数的一些临时变量等信息，然后切换到另外一个函数中执行。

 greenlet模块
“介绍”:
为了更好使用协程来完成多任务，python中的greenlet模块对其封装， 从而使得切换任务变的更加简单。
“步骤”:
1. 导入greenlet模块
2.实例化协程对象
3. 调用switch()函数